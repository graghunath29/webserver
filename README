TO DO
======

Instructions to build and package
=================================
Since this is an activator project, building and running can be done using activator ui in browser.

To package, use command "activator dist" command. A zip file is generated. This needs to be transferred to server.

Instructions to install on server
=================================
Unzip the dist zip file. 


Instructions to run on server
=============================
cd to the directory newly created when dist zip was unzipped.

In file conf/application.conf change the redisIP = localhost to correct host. 

Use the command 

bin/website -Dplay.crypto.secret=xyz -Dhttp.port=8080




